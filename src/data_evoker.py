class DataEvoker:
    def __init__(
        self,
        schema: str,
        count: int = 1,
        output_type: str = "memory",
        output_file_path: str = "",
    ) -> None:
        """ Data Evoker class. Generates sample data based on a valid schema.

        Args:
            schema (str): _description_
            count (int, optional): _description_. Defaults to 1.
            output_type (str, optional): _description_. Defaults to "memory".
            output_file_path (str, optional): _description_. Defaults to "".
        """
        pass
